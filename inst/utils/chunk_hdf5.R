# Variables for the internal structure of HDF5

gr.accs.e <- "accs/"
gr.accs.b <- "/accs"
gr.break.e = 'break/'
gr.break.b = '/break'

gr.blocks = 'blocks/'

v.idx.trust = 'trust'
v.len = 'len'
v.ref.name = 'ref'


# Alignment types

aln.type.ref='ref'
aln.type.comb='.comb'
aln.type.clean='.clean'

aln.type.pre='pre'

aln.type.msa='pan'
aln.type.add='add'

aln.type.extra1='extra1'

